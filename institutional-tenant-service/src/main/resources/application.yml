server:
  port: 8092

institutional-tenant:
  internal-api:
    token: ${INSTITUTIONAL_TENANT_INTERNAL_TOKEN:}
  monolith:
    base-url: ${INSTITUTIONAL_TENANT_MONOLITH_BASE_URL:http://localhost:8080}
    connect-timeout: 2s
    read-timeout: 5s
