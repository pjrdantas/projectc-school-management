server:
  port: 8097

dashboard-query:
  internal-api:
    token: ${DASHBOARD_QUERY_INTERNAL_TOKEN:}
  monolith:
    base-url: ${DASHBOARD_QUERY_MONOLITH_BASE_URL:http://localhost:8080}
    connect-timeout: 2s
    read-timeout: 5s
