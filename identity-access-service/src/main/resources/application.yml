server:
  port: 8091

identity-access:
  internal-api:
    token: ${IDENTITY_ACCESS_INTERNAL_TOKEN:}
  monolith:
    base-url: ${IDENTITY_ACCESS_MONOLITH_BASE_URL:http://localhost:8080}
    connect-timeout: 2s
    read-timeout: 5s
