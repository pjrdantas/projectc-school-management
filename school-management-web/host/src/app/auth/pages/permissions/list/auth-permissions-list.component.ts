import { Component, OnInit, inject } from '@angular/core'; import { CommonModule } from '@angular/common'; import { Router } from '@angular/router'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { MatIconModule } from '@angular/material/icon'; import { AccessAdminService } from '../../../services/access-admin.service'; import { Permission } from '../../../models/permission.model';
@Component({standalone:true,imports:[CommonModule,MatButtonModule,MatCardModule,MatIconModule],templateUrl:'./auth-permissions-list.component.html',styleUrls:['./auth-permissions-list.component.scss']}) export class AuthPermissionsListComponent implements OnInit{private s=inject(AccessAdminService); private r=inject(Router); permissoes:Permission[]=[]; ngOnInit(){this.s.listarPermissoes().subscribe(v=>this.permissoes=v);} novo(){this.s.selectPermission(null);this.r.navigate(['/auth/permissions/new']);} detalhe(id:string){this.s.selectPermission(id);this.r.navigate(['/auth/permissions/detail']);} editar(id:string){this.s.selectPermission(id);this.r.navigate(['/auth/permissions/edit']);} excluir(id:string){this.s.excluirPermissao(id).subscribe(()=>this.ngOnInit());}}
