import { Component, OnInit, inject } from '@angular/core'; import { CommonModule } from '@angular/common'; import { Router, RouterLink } from '@angular/router'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { AccessAdminService } from '../../../services/access-admin.service'; import { Permission } from '../../../models/permission.model';
@Component({standalone:true,imports:[CommonModule,RouterLink,MatButtonModule,MatCardModule],templateUrl:'./auth-permissions-list.component.html'}) export class AuthPermissionsListComponent implements OnInit{private s=inject(AccessAdminService); private r=inject(Router); permissoes:Permission[]=[]; ngOnInit(){this.s.listarPermissoes().subscribe(v=>this.permissoes=v);} novo(){this.r.navigate(['/auth/permissions/new']);} excluir(id:string){this.s.excluirPermissao(id).subscribe(()=>this.ngOnInit());}}
