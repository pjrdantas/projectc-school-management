import { Component, OnInit, inject } from '@angular/core'; import { CommonModule } from '@angular/common'; import { Router, RouterLink } from '@angular/router'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { AccessAdminService } from '../../../services/access-admin.service'; import { Profile } from '../../../models/profile.model';
@Component({standalone:true,imports:[CommonModule,RouterLink,MatButtonModule,MatCardModule],templateUrl:'./auth-profiles-list.component.html'}) export class AuthProfilesListComponent implements OnInit{private s=inject(AccessAdminService); private r=inject(Router); perfis:Profile[]=[]; ngOnInit(){this.s.listarPerfis().subscribe(v=>this.perfis=v);} novo(){this.r.navigate(['/auth/profiles/new']);} excluir(id:string){this.s.excluirPerfil(id).subscribe(()=>this.ngOnInit());}}
