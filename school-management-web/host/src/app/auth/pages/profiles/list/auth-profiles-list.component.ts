import { Component, OnInit, inject } from '@angular/core'; import { CommonModule } from '@angular/common'; import { Router } from '@angular/router'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { MatIconModule } from '@angular/material/icon'; import { AccessAdminService } from '../../../services/access-admin.service'; import { Profile } from '../../../models/profile.model';
@Component({standalone:true,imports:[CommonModule,MatButtonModule,MatCardModule,MatIconModule],templateUrl:'./auth-profiles-list.component.html',styleUrls:['./auth-profiles-list.component.scss']}) export class AuthProfilesListComponent implements OnInit{private s=inject(AccessAdminService); private r=inject(Router); perfis:Profile[]=[]; ngOnInit(){this.s.listarPerfis().subscribe(v=>this.perfis=v);} novo(){this.s.selectProfile(null);this.r.navigate(['/auth/profiles/new']);} detalhe(id:string){const perfil=this.perfis.find(p=>p.id===id) ?? null; this.s.selectProfileEntity(perfil); this.r.navigate(['/auth/profiles/detail']);} editar(id:string){this.s.selectProfile(id);this.r.navigate(['/auth/profiles/edit']);} excluir(id:string){this.s.excluirPerfil(id).subscribe(()=>this.ngOnInit());} isAdmin(codigo?:string){return (codigo||'').toUpperCase()==='ADMIN';}}
