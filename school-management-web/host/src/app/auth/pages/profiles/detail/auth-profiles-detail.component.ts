import { CommonModule } from '@angular/common'; import { Component, OnInit, inject } from '@angular/core'; import { ActivatedRoute, Router } from '@angular/router'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { AccessAdminService } from '../../../services/access-admin.service'; import { Profile } from '../../../models/profile.model';
@Component({standalone:true,imports:[CommonModule,MatButtonModule,MatCardModule],templateUrl:'./auth-profiles-detail.component.html'}) export class AuthProfilesDetailComponent implements OnInit{private route=inject(ActivatedRoute); private s=inject(AccessAdminService); private r=inject(Router); perfil?:Profile; ngOnInit(){const id=this.s.currentProfileId(); if(id)this.s.buscarPerfil(id).subscribe(v=>this.perfil=v);} voltar(){this.r.navigate(['/auth/profiles']);}}
