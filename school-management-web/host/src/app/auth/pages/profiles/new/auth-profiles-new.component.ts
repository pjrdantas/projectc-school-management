import { CommonModule } from '@angular/common'; import { Component, OnInit, inject, signal } from '@angular/core'; import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms'; import { ActivatedRoute, Router } from '@angular/router'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { MatFormFieldModule } from '@angular/material/form-field'; import { MatInputModule } from '@angular/material/input'; import { AccessAdminService } from '../../../services/access-admin.service';
@Component({standalone:true,imports:[CommonModule,ReactiveFormsModule,MatButtonModule,MatCardModule,MatFormFieldModule,MatInputModule],templateUrl:'./auth-profiles-new.component.html'}) export class AuthProfilesNewComponent implements OnInit{private fb=inject(FormBuilder); private s=inject(AccessAdminService); private r=inject(Router); private route=inject(ActivatedRoute); id=signal<string|null>(null); form=this.fb.group({codigo:['',Validators.required],nome:['',Validators.required],descricao:['']}); ngOnInit(){const id=this.route.snapshot.paramMap.get('id'); if(id){this.id.set(id); this.s.buscarPerfil(id).subscribe(v=>this.form.patchValue(v));}} salvar(){if(this.form.invalid)return; const obs=this.id()?this.s.atualizarPerfil(this.id()!,this.form.getRawValue() as any):this.s.criarPerfil(this.form.getRawValue() as any); obs.subscribe(()=>this.r.navigate(['/auth/profiles']));}}
