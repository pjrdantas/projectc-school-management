import { CommonModule } from '@angular/common'; import { Component, OnInit, inject } from '@angular/core'; import { ActivatedRoute, Router } from '@angular/router'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { AccessAdminService } from '../../../services/access-admin.service'; import { User } from '../../../models/user.model';
@Component({standalone:true,selector:'app-auth-users-detail',imports:[CommonModule,MatButtonModule,MatCardModule],templateUrl:'./auth-users-detail.component.html'})
export class AuthUsersDetailComponent implements OnInit{ private route=inject(ActivatedRoute); private service=inject(AccessAdminService); private router=inject(Router); usuario?:User; ngOnInit(){const id=this.route.snapshot.paramMap.get('id'); if(id)this.service.buscarUsuario(id).subscribe(u=>this.usuario=u);} voltar(){this.router.navigate(['/auth/users']);}}
